<li>
	{{name}}

	<button class="devour-burger" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}DEVOUR BURGER{{else}}Un-DEVOUR Burger{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE BURGER!</button>
</li>